apiVersion: v1
kind: Service
metadata:
  name: vote
spec:
  type: NodePort
  selector:
    app: vote
  ports:
    - protocol: TCP
      name: "vote-service"
      port: 80
      targetPort: 80
      nodePort: 30001